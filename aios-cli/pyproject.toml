[project]
name = "aios-cli"
version = "0.1.0"
description = "AIOS CLI — first client of the AIOS ecosystem"
requires-python = ">=3.12"
dependencies = [
    "typer>=0.12",
    "rich>=13.7",
    "pydantic>=2.6",
    "httpx>=0.27",
    "websockets>=12.0",
    "textual>=0.80.0",
    "duckduckgo_search>=8.0",
    "html2text>=2024.2",
    "tomlkit>=0.12.0",
    "prompt-toolkit>=3.0.0",
    "pyinstaller>=6.11.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "ruff>=0.6",
]
desktop = [
    "mss>=9.0",
    "Pillow>=10.0",
    "pygetwindow>=0.0.9",
    "pytesseract>=0.3.10",
    "pyautogui>=0.9.54",
]
voice = [
    "faster-whisper>=1.0.0",
    "numpy>=1.26.0",
]

[project.scripts]
aios = "aios.cli.main:run"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/aios"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 160
target-version = "py310"
exclude = ["src/aios/scaffold/templates/"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
